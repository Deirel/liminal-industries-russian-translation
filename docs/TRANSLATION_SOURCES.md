# Источники переводимых текстов

Набор источников каждой версии задаётся в
`translation-versions/<version>/sources.json`. Оркестратор
`build_version_delta.py` загружает конфигурацию и вызывает зарегистрированный
адаптер для каждого источника. Порядок и состав источников больше не зашиты в
основной цикл построения манифеста.

## Контракт адаптера

Адаптер возвращает:

- `records` — стабильные логические записи с исходным текстом, хэшем,
  местоположением и форматом результата;
- `source_files` — все файлы и члены архивов, от которых зависит извлечение;
- `report` — счётчики, специфичные для источника.

Оркестратор добавляет к каждой записи `source_id` и `source_type`, проверяет
уникальность ID и объединяет отчёты. Общие форматы результата:

- `lang` — ключ в `assets/<namespace>/lang/ru_ru.json`;
- `patchouli_json` — поле локализованного JSON Patchouli;
- `manual_text` — строка текстового руководства Immersive Engineering;
- `mantle_book_json` — видимое поле книги Mantle;
- FTB Quests — структурная замена поля в SNBT.

## Включённые источники

- `ftb_quests` — заголовки и описания FTB Quests;
- `registry_items` — реальные предметы реестра;
- `registry_blocks` — самостоятельный реестр блоков;
- `patchouli` — метаданные, категории, статьи и страницы Patchouli;
- `immersive_engineering_manual` — текстовое руководство Immersive Engineering;
- `mantle_books` — книги Mantle, включая Tinkers' Construct.

Блоки не выводятся из списка предметов. Для Rescripted используется тип
ProbeJS `Block`; для Original предусмотрен fallback по всем blockstate-моделям
и блокам из startup-скриптов KubeJS. Наличие английского lang-ключа не является
условием сбора: его отсутствие само по себе должно быть заметно в дельте.

## Patchouli

Адаптер просматривает:

```text
data/<namespace>/patchouli_books/<book>/book.json
assets/<namespace>/patchouli_books/<book>/en_us/**/*.json
```

Поиск выполняется как в основном JAR мода, так и во вложенных
`META-INF/jarjar/*.jar`. Это важно для модов вроде Thermal Series, где
языковые файлы и книга находятся во внутреннем core-JAR.

Поля `name`, `title`, `subtitle`, `description`, `landing_text`, `text` и
`link_text` считаются видимыми. Если значение является ключом из `en_us`, оно
попадает в обычный `ru_ru.json`. Литеральный текст записывается в зеркальный
JSON под каталогом `ru_ru`. Ссылки, рецепты, иконки, типы страниц и прочая
структура копируются без изменений.

Значения шаблонов Patchouli, начинающиеся с `#`, не считаются текстом.
Разметка Patchouli остаётся частью исходной строки и проверяется при
утверждении перевода.

## Ревью штатных книг

Источник с `"review_native": true` не считает наличие `ru_ru` достаточным
покрытием. Адаптер сохраняет штатный текст как неутверждённую подсказку и
выставляет один из статусов:

- `REVIEW_NATIVE` — строка существует и структурно актуальна;
- `MISSING_NATIVE` — русского файла или поля нет;
- `INVALID_NATIVE` — русский ресурс не разбирается;
- `STALE_NATIVE` — структура, строки или управляющие маркеры отстают от
  английского ресурса.

Статус определяется для ресурса целиком до создания записей. Поэтому одна
страница или статья не может одновременно содержать `REVIEW_NATIVE` и
`STALE_NATIVE`. Для устаревшего ресурса штатные строки не используются как
подсказки: при смещении строк или элементов массива их соответствие исходнику
нельзя гарантировать.

Перед этой классификацией оркестратор ищет точную утверждённую пару
`ID + source_hash`. Совпадение всегда получает `FINALIZED`; изменение только
штатного русского текста не возвращает его в дельту.

`pending.tsv` содержит исходный статус, английский текст и
`native_translation`, если её удалось безопасно извлечь. Штатная строка
заполняет рабочую колонку `translation` как драфт, но становится частью
каталога только после обычного независимого ревью.

## Immersive Engineering

Адаптер читает:

```text
assets/immersiveengineering/manual/en_us/*.txt
assets/immersiveengineering/manual/ru_ru/*.txt
```

Непустые видимые строки получают ID вида
`ie-manual:<namespace>:<file>:line:<index>`. Сравниваются число строк и
управляющие маркеры каждой соответствующей строки. Текст подписи внутри
`<link;...>` переводим, а адрес и якорь ссылки должны сохраняться. Строка,
состоящая только из управляющих маркеров, например `<&...>`, `<np>` или
`<br>`, остаётся частью шаблона и не создаёт запись дельты.

Сборщик всегда начинает с английского файла и заменяет только утверждённые
строки. Поэтому устаревшие абзацы и удалённые страницы штатного перевода не
переносятся в результат.

## Mantle

Адаптер читает:

```text
assets/<namespace>/book/<book>/en_us/**/*.json
assets/<namespace>/book/<book>/ru_ru/**/*.json
assets/<namespace>/book/<book>/en_us/language.lang
assets/<namespace>/book/<book>/ru_ru/language.lang
```

Для Tinkers' Construct переводимыми считаются `title`, `text`, `subText`,
`effects`, `properties` и `tooltip`, включая строки в массивах, а также
название группы в `data` у операции `add_group`. Registry ID, `modifier_id`,
`tool_filter`, пути изображений, рецепты и остальные поля операций индекса
остаются техническими.

ID имеет вид
`mantle-book:<namespace>:<book>:<file>:<json_pointer>`. Русский JSON
разбирается строго. Для сравнения все непустые переводимые строки в обоих
документах заменяются одинаковым маркером, после чего сравнивается весь JSON:
поля, типы, массивы и технические значения. Если ресурс невалиден или отстаёт
от английской структуры, сборщик создаёт новый валидный JSON из английского
шаблона и утверждённых переводов. Пустые и состоящие только из пробелов строки
не создают записей дельты.

Файл `language.lang` является отдельной таблицей `key=value`, которую Mantle
использует для подписей разделов, групп материалов и других автоматически
построенных элементов. Значение переводится, а ключ остаётся техническим. ID
имеет вид `mantle-book-language:<namespace>:<book>:<key>`. Если ключ есть
только в английской таблице, запись обязательно попадает в дельту: иначе
Mantle выводит сам ключ, например `melee_harvest_materials`. При сборке
русская таблица создаётся целиком, чтобы частичный ресурс не скрыл штатные
русские значения.

## Добавление источника

1. Реализовать collector, возвращающий `SourceResult`.
2. Зарегистрировать имя адаптера в `build_version_delta.py`.
3. Если появляется новый формат результата, добавить builder в
   `build_version_resources.py`.
4. Добавить источник в `sources.json` нужных версий.
5. Для runtime-проверяемого источника добавить `AuditProvider` с таким же ID.
6. Назначить новые namespace выходным resource pack в `resource-packs.json`.
7. Добавить fixture-тест извлечения и построения ресурсов.

Audit-мод получает `sources.json` выбранной версии при сборке. Источник с
`"audit": false` намеренно не требует runtime-провайдера. Для остальных
отсутствующий провайдер или нулевой результат считается ошибкой аудита.

Для `immersive_engineering_manual` runtime-провайдер проверяет наличие
эффективного русского TXT и соответствие управляющих маркеров. Для
`tconstruct_books` он строго разбирает эффективные русский JSON и
`language.lang`, проверяет каждое видимое поле из того же набора, что
использует статический адаптер, и требует русский результат для каждого
английского ключа таблицы Mantle.

## Незавершённые переводы

Новый источник может сразу создать pending-записи. Существующие переводы из
payload используются как подсказки в `work/pending.tsv`. Проверенную часть
можно переносить в каталог независимо:

```sh
python3 scripts/approve_version_translations.py \
  --version <version> \
  --allow-partial
```

Payload остаётся последней завершённой сборкой, пока в манифесте есть pending.

При утверждении `block_name` скрипт сопоставляет `block_id` с `item_id` в том
же манифесте. Если для item-записи текущего исходного текста уже есть перевод
в неизменяемом каталоге (или он утверждается в том же пакете), перевод блока
обязан совпадать с ним буквально.
