# Чекпоинт 4: проверка Rescripted

Дата проверки: 2026-08-05.

Проверены все 22 659 записей эффективного реестра Rescripted:

- 9 322 названия предметов;
- 6 989 названий блоков;
- 841 строка квестов;
- 2 184 строки Patchouli;
- 664 строки руководства Immersive Engineering;
- 2 659 строк книг Tinkers' Construct.

Сначала выполнена механическая проверка пустых видимых значений, структуры
TSV, дубликатов, `ID + source_hash`, старого текста, разметки и технических
токенов рекомендаций. Затем записи прочитаны по английскому источнику и
контексту: названия сгруппированы по namespace и семействам ключей, квесты и
книги — по source и расположению страницы. Три непересекающихся среза покрыли
22 656 видимых записей; 3 исходных поля явно заменены структурным override.
Для каждой видимой записи сохранён отдельный исходный verdict с идентификатором
ревьюера и методом проверки; итоговый реестр не создаёт `PASS` по умолчанию.

Итоговый реестр содержит 22 464 `PASS` и 195 `CHANGE`. Все 195 рекомендаций
повторно прочитаны независимым ревьюером: 195 одобрены, 0 отклонены, 2
отредактированы до утверждения. Незакрытых и неутверждённых замечаний нет.
Независимый след хранит исходную и итоговую рекомендацию, примечание и задачу
ревьюера; `change-review-summary.json` фиксирует `gpt-5.6-terra / medium`.

Артефакты:

- `work/quality-review.tsv`: SHA-256
  `672aa57359d1e23ab1238cfd7f71cfb010acea6d2a8bcc1333c383a54109942d`;
- `work/quality-review-summary.json`: SHA-256
  `500ba182a63f07eb53b95fdf460b834c33969b78425b97d9ca6aeb873c3a3168`;
- `work/quality-audit-2026-08-05/approved-changes.tsv`: SHA-256
  `73ec93f3126079a0dcb41fd9abc37ec7c0edec5a0e2ec276039ca0d24e280546`;
- `work/quality-audit-2026-08-05/change-review-summary.json`: SHA-256
  `adc6313b80de6d92c131115d6921872c74aa01fd7598dff604db5cbc504c66a4`.

`build_quality_review.py --check` воспроизводит полный реестр, требует входной
verdict для каждой видимой записи и `independent_review=APPROVED` с provenance
для каждого `CHANGE`. Команда
`PYTHONPATH=scripts python3 -m unittest discover -s scripts/tests -p 'test_*.py'`
выполняет все 60 Python-тестов без ошибок.
